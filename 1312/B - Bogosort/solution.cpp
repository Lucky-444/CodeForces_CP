#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;        while (t--) {        int n;        cin >> n;                vector<int> a(n);        for (auto &x : a) cin >> x;                // Sort in descending order        sort(a.rbegin(), a.rend());                for (auto x : a) cout << x << " ";        cout << endl;    }}