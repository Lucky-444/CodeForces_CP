#include <bits/stdc++.h>using namespace std; bool isFair(long long x) {    long long temp = x;        while (temp > 0) {        int d = temp % 10;        temp /= 10;                if (d != 0 && x % d != 0) {            return false;        }    }        return true;} int main() {    int T;    cin >> T;        while (T--) {        long long n;        cin >> n;                while (!isFair(n)) {            n++;        }                cout << n << "\n";    }}