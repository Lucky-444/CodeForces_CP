#include <bits/stdc++.h>using namespace std;using ll = long long; int main() {     int t;    cin >> t;     while(t--) {         ll n;        cin >> n;         ll d = -1;         // Find the smallest divisor of n (greater than 1)        for(ll i = 2; i * i <= n; i++){            if(n % i == 0){                d = i;                break;            }        }         // If no divisor found, n is prime        // Only possible split with maximum gcd is:        // 1 and (n-1)        if(d == -1){            cout << 1 << " " << n-1 << "\n";        }        else{            /*            If d is the smallest divisor of n, then                 n = d * g             where g = n / d.             Because divisors come in pairs:                (d , n/d)             Since d is the smallest divisor > 1,            n/d must be the largest divisor < n.             Example:                n = 12                 divisors = 1 2 3 4 6 12                 smallest divisor >1 = 2                largest divisor <12 = 12/2 = 6             So we take:                a = g                b = n - g             This maximizes gcd(a,b), which minimizes            LCM(a,b) = (a*b)/gcd(a,b)            */             ll g = n / d;             cout << g << " " << n - g << "\n";        }    }}