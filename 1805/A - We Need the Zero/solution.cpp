#include <bits/stdc++.h>using namespace std; int main() {    int T ;    cin >> T;        while(T --){        // your code goes here        int n ;         cin >> n;                 vector<int>vec(n);        for(auto &x : vec){            cin >> x;        }                if((n % 2)== 0){            int xr = 0;                    for(auto it : vec){                xr ^= it;            }                        if(xr == 0){                cout << 1 << endl;                continue;            }else{                cout << -1 << endl;                continue ;            }                    }else{            int xr = 0;                    for(auto it : vec){                xr ^= it;            }                        cout << xr << endl;                    }     }}