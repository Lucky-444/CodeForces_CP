#include <bits/stdc++.h>using namespace std;using ll = long long ;  int main() {	// your code goes here	int T ; 	cin >> T ;		while(T --){	    int n ; 	    cin >> n ;	    	    vector<int>a(n);	    for(auto &x : a){	        cin >> x;	    }	    	    int cnt = 0 ;	    int maxi = 0;	    	    for(auto it : a){	        if(it == 0){	            cnt ++;      	        }else{	            maxi = max(cnt , maxi);	            cnt = 0;	        }	    }	    	    maxi = max(cnt , maxi);	   	    cout << maxi << endl;	    	} }