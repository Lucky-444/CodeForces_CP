#include <bits/stdc++.h>using namespace std;using ll = long long ;  int main() {	// your code goes here	int T ; 	cin >> T ;		while(T --){	    int n ; 	    cin >> n ;	    	    vector<int>a(n);	    for(auto &x : a){	        cin >> x;	    }	    	    vector<int>b(n);	    	    for(int i = 0 ; i < n ; i ++){	        int val = (n + 1) - a[i];	        	        b[i] = val;	    }	    	    for(auto it : b){	        cout << it << " " ;	    }	    	    cout << endl;	    	} }