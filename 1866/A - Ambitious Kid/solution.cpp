#include <bits/stdc++.h>using namespace std; int main() {	// your code goes here	int n ;	cin >> n ; 		vector<int>vec(n);	int mini = INT_MAX;		for(int i = 0 ; i < n ; i ++){	    cin >> vec[i];	    	    mini = min(mini , abs(vec[i]));	    	}		cout << mini << endl ; }