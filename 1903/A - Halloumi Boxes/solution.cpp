#include <bits/stdc++.h>using namespace std;bool isSorted(auto &vec){    int n = vec.size();        for(int i = 1 ; i < n ; i++){        if(vec[i] < vec[i - 1]){            return false;        }    }    return true;}  int main() {	// your code goes here	int T ; 	cin >> T ; 		while(T --){	    int n  , k; 	    cin >> n >> k;	    	    vector<int>vec(n);	    for(int i = 0 ; i < n ; i ++){	        cin >> vec[i];	    }	    	    if(isSorted(vec)){	        cout << "Yes" << endl;	        continue ;	    }	    	    if(k < 2){	        cout << "No" << endl;	    }else{	        cout << "Yes" << endl ;	    }	}		return 0 ; }