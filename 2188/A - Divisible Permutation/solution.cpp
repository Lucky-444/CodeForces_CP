#include <bits/stdc++.h>using namespace std; int main() {	// your code goes here	int T ;	cin >> T ;		while(T --){	    int n; 	    cin >> n ;	    	    vector<int>vec(n);	    	    int cnt = 1 ; 	    	    for(int i = n - 1 ; i >= 0 ; i -= 2){	        vec[i] = cnt ++;	    }	    	    cnt = n;	    for(int i = n - 2 ; i >= 0 ; i -= 2){	        vec[i] = cnt --;	    }	    	    for(auto it : vec){	        cout << it << " " ;	        	    }	    	    cout << endl; 	    	} }