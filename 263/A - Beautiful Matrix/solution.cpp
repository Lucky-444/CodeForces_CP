#include <bits/stdc++.h>using namespace std;using ll = long long; int main() {    //your code goes here        vector<vector<int>> a(5, vector<int>(5));     for (int i = 0; i < 5; i++) {        for (int j = 0; j < 5; j++) {            cin >> a[i][j];        }    }     int r, c;     for (int i = 0; i < 5; i++) {        for (int j = 0; j < 5; j++) {            if (a[i][j] == 1) {                r = i;                c = j;            }        }    }     cout << abs(r - 2) + abs(c - 2);}